# assets/

Place your photo here as `photo.jpg` (square works best — the hero uses a circular mask).

If `photo.jpg` is missing the site will automatically fall back to a large "R" monogram in the hero frame.
